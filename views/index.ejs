<%- include("partials/header.ejs") %>

<div class="sort">
  <p>Sort By:</p>
  <form action="/sort" method="post">
    <select name="sort" id="sort-option">
      <option value="title">Title</option>
      <option value="author">Author</option>
      <option value="rating">Rating</option>
    </select>
    <input type="submit" value="Go" />
  </form>
</div>

<div class="books background-cover">
  <% if ( locals.data.length > 0) { %>
    <% locals.data.forEach((book) => { %> 
      <a href="/book?key=<%= book.key %>&title=<%= book.title %>&author=<%= book.author %>&cover=<%= book.cover_id %>&year=<%= book.year %>"><div class="book-info">
        <h3><%= book.title %></h3>
        <h4><%= book.author %></h4>
        <p><strong><%= book.year %></strong></p>
        <p><%= book.review_text %></p>
        
      </div>
    </a>
    <% }) %>
  <% } else { %>
    <div id="empty-message"><%= noBookMessage %></div>
  <% } %>
</div>

</main>



<%- include("partials/footer.ejs") %>
